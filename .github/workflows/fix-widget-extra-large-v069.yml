name: Legacy Widget Extra Large Sizing v0.6.9

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This legacy v0.6.9 widget mutation workflow is retired."
          echo "Current Android builds must use the golden v1.0.11 lineage workflows instead."
