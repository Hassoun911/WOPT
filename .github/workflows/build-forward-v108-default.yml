name: Legacy v1.0.8 Forward APK (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This v1.0.8 workflow is retired."
          echo "Use Build v1.0.12 From v1.0.11 Golden for current Android APK work."
