name: Validate Web Masjid TV

on:
  push:
    branches:
      - release/v1.0.14-masjid-tv-fixes
    paths:
      - 'pwa/app/masjid-tv/**'
      - 'pwa/app/WebMasjidTvMenuEnhancer.tsx'
      - 'pwa/app/layout.tsx'
      - '.github/workflows/validate-web-masjid-tv.yml'
  workflow_dispatch:

jobs:
  build-web:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: pwa
    steps:
      - uses: actions/checkout@v6
        with:
          ref: release/v1.0.14-masjid-tv-fixes
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: pwa/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Verify TV source wiring
        run: |
          grep -q 'Masjid Display Studio' app/masjid-tv/page.tsx
          grep -q 'Masjid TV / Big Screen' app/WebMasjidTvMenuEnhancer.tsx
          grep -q 'WebMasjidTvMenuEnhancer' app/layout.tsx
          grep -q 'layout-announcements' app/masjid-tv/masjid-tv.css
          grep -q 'Enter Full Screen' app/masjid-tv/page.tsx
      - name: Build website using production script
        run: npm run build
