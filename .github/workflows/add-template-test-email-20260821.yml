name: Retired - Add Template Test Email 2026-08-21

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow notice
        run: |
          echo "This legacy one-off email template workflow is retired."
          echo "Current Hassoun admin/email changes are managed by the active mainline Worker and PWA workflows."
