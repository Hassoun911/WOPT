import fs from 'node:fs';

const path = 'src/quran/AlHafizClassroom.tsx';
let s = fs.readFileSync(path, 'utf8');

const typeNeedle = 'type SourceMode = "range" | "surah" | "page" | "paste";\n';
if (!s.includes(typeNeedle)) throw new Error('SourceMode anchor not found');
s = s.replace(typeNeedle, typeNeedle + `type CardFontStyle = "mushaf" | "classic" | "clean";\ntype CardViewSettings = { fontSize: number; fontStyle: CardFontStyle; translation: boolean };\ntype StudyQuiz = { mode: "blank" | "abc"; prompt: string; options: string[]; answer: string; selected?: string };\n`);

const keyNeedle = 'const OLD_ATTEMPTS_KEY = "wopt:quran:smart-memorize:attempts:v1";\n';
if (!s.includes(keyNeedle)) throw new Error('storage key anchor not found');
s = s.replace(keyNeedle, keyNeedle + 'const CARD_VIEW_KEY = "wopt:quran:al-hafiz:card-view:v1";\n');

const stateNeedle = '  const [latestAttempt, setLatestAttempt] = useState<Attempt | null>(null);\n';
if (!s.includes(stateNeedle)) throw new Error('state anchor not found');
s = s.replace(stateNeedle, stateNeedle + `  const [cardViewSettings, setCardViewSettings] = useState<Record<string, CardViewSettings>>({});\n  const [translations, setTranslations] = useState<Record<string, string>>({});\n  const [translationLoading, setTranslationLoading] = useState(false);\n  const [studyQuiz, setStudyQuiz] = useState<StudyQuiz | null>(null);\n`);

const derivedNeedle = '  const activeCard = activeLesson?.cards[clamp(activeLesson.currentCard, 0, Math.max(0, activeLesson.cards.length - 1))] ?? null;\n';
if (!s.includes(derivedNeedle)) throw new Error('activeCard anchor not found');
s = s.replace(derivedNeedle, derivedNeedle + `  const activeCardKey = activeLesson && activeCard ? activeLesson.id + ":" + activeCard.id : "";\n  const activeCardView = activeCardKey ? (cardViewSettings[activeCardKey] ?? { fontSize: 28, fontStyle: "mushaf" as CardFontStyle, translation: false }) : { fontSize: 28, fontStyle: "mushaf" as CardFontStyle, translation: false };\n`);

const effectNeedle = '  useEffect(() => {\n    if (!loaded) return;\n    if (activeId) void AsyncStorage.setItem(ACTIVE_KEY, activeId);\n    else void AsyncStorage.removeItem(ACTIVE_KEY);\n  }, [activeId, loaded]);\n';
if (!s.includes(effectNeedle)) throw new Error('effect anchor not found');
s = s.replace(effectNeedle, effectNeedle + `\n  useEffect(() => {\n    void AsyncStorage.getItem(CARD_VIEW_KEY).then((saved) => {\n      if (!saved) return;\n      try { setCardViewSettings(JSON.parse(saved) as Record<string, CardViewSettings>); } catch {}\n    });\n  }, []);\n\n  useEffect(() => {\n    if (!loaded) return;\n    void AsyncStorage.setItem(CARD_VIEW_KEY, JSON.stringify(cardViewSettings));\n  }, [cardViewSettings, loaded]);\n`);

const playNeedle = `  function playAyahs(ayahs: QuranAyah[]) {\n    if (!QuranAudio || !ayahs.length) return;\n    const first = ayahs[0]!;\n    const last = ayahs[ayahs.length - 1]!;\n    QuranAudio.playRange(absoluteIndex(first.surah, first.ayah) + 1, absoluteIndex(last.surah, last.ayah) + 1, "ar.alafasy", 128, "Mishary Alafasy", false, 1);\n  }\n`;
if (!s.includes(playNeedle)) throw new Error('playAyahs anchor not found');
const playReplacement = `  function playAyahs(ayahs: QuranAyah[], repeat = false) {\n    if (!QuranAudio || !ayahs.length) return;\n    const first = ayahs[0]!;\n    const last = ayahs[ayahs.length - 1]!;\n    QuranAudio.playRange(absoluteIndex(first.surah, first.ayah) + 1, absoluteIndex(last.surah, last.ayah) + 1, "ar.alafasy", 128, "Mishary Alafasy", repeat, 1);\n  }\n\n  function updateCardView(patch: Partial<CardViewSettings>) {\n    if (!activeCardKey) return;\n    setCardViewSettings((current) => ({ ...current, [activeCardKey]: { ...activeCardView, ...patch } }));\n  }\n\n  function quranWordAudioUrl(word: LessonWord) {\n    const pad = (value: number) => String(value).padStart(3, "0");\n    return \`https://audio.qurancdn.com/wbw/\${pad(word.surah)}_\${pad(word.ayah)}_\${pad(word.wordIndex)}.mp3\`;\n  }\n\n  async function playWord(word: LessonWord) {\n    if (!QuranAudio) return;\n    try { await QuranAudio.play(quranWordAudioUrl(word), 1); } catch (error) {\n      Alert.alert(t("Word audio unavailable", "تعذر تشغيل الكلمة"), error instanceof Error ? error.message : String(error));\n    }\n  }\n\n  async function ensureCardTranslations() {\n    if (!activeCard || translationLoading) return;\n    const missing = activeCard.ayahs.filter((ayah) => !translations[ayah.surah + ":" + ayah.ayah]);\n    if (!missing.length) return;\n    setTranslationLoading(true);\n    try {\n      const pairs = await Promise.all(missing.map(async (ayah) => {\n        const response = await fetch(\`https://api.alquran.cloud/v1/ayah/\${ayah.surah}:\${ayah.ayah}/en.sahih\`);\n        const json = await response.json() as { data?: { text?: string } };\n        return [ayah.surah + ":" + ayah.ayah, json.data?.text ?? ""] as const;\n      }));\n      setTranslations((current) => ({ ...current, ...Object.fromEntries(pairs) }));\n    } catch {\n      Alert.alert(t("Translation unavailable", "الترجمة غير متاحة"), t("Please check your internet connection and try again.", "تحقق من اتصال الإنترنت وحاول مرة أخرى."));\n    } finally { setTranslationLoading(false); }\n  }\n\n  function toggleTranslation() {\n    const next = !activeCardView.translation;\n    updateCardView({ translation: next });\n    if (next) void ensureCardTranslations();\n  }\n\n  function splitActiveCard() {\n    if (!activeLesson || !activeCard) return;\n    if (activeCard.ayahs.length < 2) {\n      Alert.alert(t("Card is already very small", "البطاقة صغيرة بالفعل"), t("This card contains one ayah, so it cannot be divided by ayah any further.", "هذه البطاقة تحتوي على آية واحدة ولا يمكن تقسيمها أكثر حسب الآيات."));\n      return;\n    }\n    const middle = Math.ceil(activeCard.ayahs.length / 2);\n    const leftAyahs = activeCard.ayahs.slice(0, middle);\n    const rightAyahs = activeCard.ayahs.slice(middle);\n    const stamp = Date.now();\n    const left: LessonCard = { id: activeCard.id + "-a-" + stamp, title: activeCard.title + "A", ayahs: leftAyahs, words: lessonWords(leftAyahs) };\n    const right: LessonCard = { id: activeCard.id + "-b-" + stamp, title: activeCard.title + "B", ayahs: rightAyahs, words: lessonWords(rightAyahs) };\n    updateActiveLesson((lesson) => {\n      const cards = [...lesson.cards];\n      cards.splice(lesson.currentCard, 1, left, right);\n      return { ...lesson, cards };\n    });\n    setStudyQuiz(null);\n  }\n\n  function shuffle<T>(items: T[]) {\n    return [...items].sort(() => Math.random() - 0.5);\n  }\n\n  function buildQuiz(mode: "blank" | "abc") {\n    if (!activeCard || activeCard.words.length < 2) return;\n    const words = activeCard.words;\n    if (mode === "blank") {\n      const index = Math.min(words.length - 1, Math.max(0, Math.floor(words.length / 2)));\n      const answer = words[index]!.text;\n      const start = Math.max(0, index - 5);\n      const end = Math.min(words.length, index + 6);\n      const prompt = words.slice(start, end).map((word, local) => start + local === index ? "_____" : word.text).join(" ");\n      const distractors = shuffle(words.filter((word) => word.text !== answer).map((word) => word.text)).slice(0, 3);\n      setStudyQuiz({ mode, prompt, answer, options: shuffle([answer, ...distractors]) });\n      return;\n    }\n    const index = Math.min(words.length - 1, Math.max(1, Math.floor(words.length / 2)));\n    const answer = words[index]!.text;\n    const previous = words[index - 1]!.text;\n    const distractors = shuffle(words.filter((word) => word.text !== answer).map((word) => word.text)).slice(0, 2);\n    setStudyQuiz({ mode, prompt: t(\`What comes after “\${previous}”?\`, \`ما الكلمة التي تأتي بعد «\${previous}»؟\`), answer, options: shuffle([answer, ...distractors]) });\n  }\n\n  function answerQuiz(option: string) {\n    if (!studyQuiz || studyQuiz.selected) return;\n    setStudyQuiz({ ...studyQuiz, selected: option });\n  }\n`;
s = s.replace(playNeedle, playReplacement);

const cardRegex = /\{activeCard \? <View style=\{styles\.flashcard\}>[\s\S]*?<\/View> : null\}\n\n            \{!testScope \?/;
if (!cardRegex.test(s)) throw new Error('flashcard render block not found');
const newCard = `{activeCard ? <View style={styles.flashcard}>\n              <View style={styles.flashcardTop}><Text style={styles.flashcardLabel}>🗂️ {t("CARD", "بطاقة")} {activeLesson.currentCard + 1}/{activeLesson.cards.length}</Text><Text style={styles.flashcardScore}>⭐ {t("Best", "أفضل")}: {bestCard}%</Text></View>\n\n              {!testScope ? <View style={styles.cardToolPanel}>\n                <View style={styles.cardToolRow}>\n                  <Text style={styles.cardToolLabel}>🔤 {t("Font size", "حجم الخط")}</Text>\n                  <Pressable style={styles.fontStep} onPress={() => updateCardView({ fontSize: Math.max(20, activeCardView.fontSize - 2) })}><Text style={styles.fontStepText}>A−</Text></Pressable>\n                  <Text style={styles.fontSizeValue}>{activeCardView.fontSize}</Text>\n                  <Pressable style={styles.fontStep} onPress={() => updateCardView({ fontSize: Math.min(48, activeCardView.fontSize + 2) })}><Text style={styles.fontStepText}>A＋</Text></Pressable>\n                </View>\n                <View style={styles.fontStyleRow}>\n                  {(["mushaf", "classic", "clean"] as CardFontStyle[]).map((font) => <Pressable key={font} onPress={() => updateCardView({ fontStyle: font })} style={[styles.fontStyleButton, activeCardView.fontStyle === font && styles.fontStyleButtonActive]}><Text style={[styles.fontStyleText, activeCardView.fontStyle === font && styles.fontStyleTextActive]}>{font === "mushaf" ? t("Mushaf", "مصحف") : font === "classic" ? t("Classic", "تقليدي") : t("Clean", "واضح")}</Text></Pressable>)}\n                </View>\n                <View style={styles.cardFeatureRow}>\n                  <Pressable onPress={toggleTranslation} style={[styles.cardFeature, activeCardView.translation && styles.cardFeatureActive]}><Text style={[styles.cardFeatureText, activeCardView.translation && styles.cardFeatureTextActive]}>🌐 {t("Translation", "ترجمة")}</Text></Pressable>\n                  <Pressable onPress={splitActiveCard} style={styles.cardFeature}><Text style={styles.cardFeatureText}>✂️ {t("Split card", "قسّم البطاقة")}</Text></Pressable>\n                </View>\n              </View> : null}\n\n              {testScope ? <View style={styles.hiddenTest}><Text style={styles.hiddenMic}>🎙️</Text><Text style={styles.hiddenTitle}>{t("Text hidden — recite from memory", "النص مخفي — سمّع من حفظك")}</Text><Text style={styles.hiddenSub}>{speechStatus.partialTranscript || speechStatus.transcript || t("I'm listening…", "أنا أستمع…")}</Text></View> : activeLesson.tajweed ? (\n                <View style={styles.tajweedPageWrap}>\n                  {Array.from(new Set(activeCard.ayahs.map((ayah) => pageForAyah(ayah.surah, ayah.ayah) ?? 1))).map((page) => <QuranPageText key={page} page={page} ayahs={activeCard.ayahs.filter((ayah) => (pageForAyah(ayah.surah, ayah.ayah) ?? 1) === page)} appearance={{ ...appearance, tajweed: true, pageTheme: "white", textColor: "#183d33", fontSize: activeCardView.fontSize }} locale={locale} onPressAyah={() => {}} />)}\n                </View>\n              ) : <View style={styles.interactiveAyahList}>\n                <Text style={styles.tapWordHint}>👆 {t("Tap any word to hear that word only", "اضغط على أي كلمة لسماعها وحدها")}</Text>\n                {activeCard.ayahs.map((ayah) => <View key={ayah.surah + ":" + ayah.ayah} style={styles.interactiveAyah}>\n                  <View style={styles.wordWrap}>\n                    {tokenizeAyah(ayah).map((word) => <Pressable key={word.surah + ":" + word.ayah + ":" + word.wordIndex} onPress={() => void playWord(word)} style={styles.wordButton}><Text style={[styles.wordButtonText, { fontSize: activeCardView.fontSize, lineHeight: Math.round(activeCardView.fontSize * 1.7), fontFamily: activeCardView.fontStyle === "mushaf" ? "serif" : activeCardView.fontStyle === "classic" ? "sans-serif" : undefined, fontWeight: activeCardView.fontStyle === "clean" ? "700" : "500" }]}>{word.text}</Text></Pressable>)}\n                    <Text style={[styles.ayahNumber, { fontSize: Math.max(16, activeCardView.fontSize - 8) }]}>﴿{ayah.ayah}﴾</Text>\n                  </View>\n                  {activeCardView.translation ? <View style={styles.translationBox}><Text style={styles.translationLabel}>{translationLoading && !translations[ayah.surah + ":" + ayah.ayah] ? t("Loading translation…", "جاري تحميل الترجمة…") : t("Meaning", "المعنى")}</Text><Text style={styles.translationText}>{translations[ayah.surah + ":" + ayah.ayah] || t("Translation will appear here.", "ستظهر الترجمة هنا.")}</Text></View> : null}\n                </View>)}\n              </View>}\n\n              {!testScope ? <View style={styles.studyActions}>\n                <Pressable style={styles.studyButton} onPress={() => playAyahs(activeCard.ayahs)}><Text style={styles.studyButtonText}>▶️ {t("Play card", "تشغيل البطاقة")}</Text></Pressable>\n                <Pressable style={styles.studyButton} onPress={() => playAyahs(activeCard.ayahs, true)}><Text style={styles.studyButtonText}>🔁 {t("Loop card", "تكرار البطاقة")}</Text></Pressable>\n                <Pressable style={styles.testButton} onPress={() => void startTest("card")}><Text style={styles.testButtonText}>🎙️ {t("Recite", "تسميع")}</Text></Pressable>\n              </View> : <Pressable style={styles.stopButton} onPress={stopTest}><Text style={styles.stopButtonText}>⏹ {t("I'm done — grade me", "انتهيت — قيّمني")}</Text></Pressable>}\n\n              {!testScope ? <View style={styles.quizPanel}>\n                <View style={styles.quizHeader}><Text style={styles.quizTitle}>🧠 {t("Test me", "اختبرني")}</Text><Text style={styles.quizSub}>{t("Choose a quick practice style", "اختر طريقة اختبار سريعة")}</Text></View>\n                <View style={styles.quizModeRow}>\n                  <Pressable onPress={() => buildQuiz("blank")} style={styles.quizMode}><Text style={styles.quizModeText}>✏️ {t("Fill the blank", "املأ الفراغ")}</Text></Pressable>\n                  <Pressable onPress={() => buildQuiz("abc")} style={styles.quizMode}><Text style={styles.quizModeText}>🔤 {t("ABC question", "سؤال اختياري")}</Text></Pressable>\n                </View>\n                {studyQuiz ? <View style={styles.quizQuestion}>\n                  <Text style={styles.quizQuestionType}>{studyQuiz.mode === "blank" ? t("FILL THE BLANK", "املأ الفراغ") : t("CHOOSE THE ANSWER", "اختر الإجابة")}</Text>\n                  <Text style={styles.quizPrompt}>{studyQuiz.prompt}</Text>\n                  <View style={styles.quizOptions}>{studyQuiz.options.map((option, index) => { const selected = studyQuiz.selected === option; const correct = option === studyQuiz.answer; return <Pressable key={option + index} onPress={() => answerQuiz(option)} style={[styles.quizOption, studyQuiz.selected && correct && styles.quizOptionCorrect, selected && !correct && styles.quizOptionWrong]}><Text style={[styles.quizOptionText, studyQuiz.selected && correct && styles.quizOptionTextCorrect]}>{String.fromCharCode(65 + index)}. {option}</Text></Pressable>; })}</View>\n                  {studyQuiz.selected ? <Text style={[styles.quizFeedback, studyQuiz.selected === studyQuiz.answer ? styles.quizFeedbackGood : styles.quizFeedbackTry]}>{studyQuiz.selected === studyQuiz.answer ? t("✓ Correct — Masha’Allah!", "✓ صحيح — ما شاء الله!") : t("Try again — correct answer: ", "حاول مرة أخرى — الإجابة الصحيحة: ") + studyQuiz.answer}</Text> : null}\n                  <Pressable onPress={() => buildQuiz(studyQuiz.mode)} style={styles.nextQuizButton}><Text style={styles.nextQuizText}>↻ {t("Another question", "سؤال آخر")}</Text></Pressable>\n                </View> : null}\n              </View> : null}\n            </View> : null}\n\n            {!testScope ?`;
s = s.replace(cardRegex, newCard);

const styleAnchor = '  hiddenTest: { minHeight: 190, alignItems: "center", justifyContent: "center", padding: 18 },';
if (!s.includes(styleAnchor)) throw new Error('style insertion anchor not found');
const newStyles = `  cardToolPanel: { marginTop: 12, borderRadius: 15, backgroundColor: "#f4f0e5", borderWidth: 1, borderColor: "#ddd2b9", padding: 10, gap: 8 },\n  cardToolRow: { flexDirection: "row", alignItems: "center", gap: 8 },\n  cardToolLabel: { flex: 1, color: "#435c53", fontSize: 10.5, fontWeight: "900" },\n  fontStep: { minWidth: 42, borderRadius: 10, backgroundColor: "#fff", borderWidth: 1, borderColor: "#cfc2a7", paddingVertical: 7, alignItems: "center" },\n  fontStepText: { color: "#2b5b4c", fontSize: 11, fontWeight: "900" },\n  fontSizeValue: { color: "#8b6e31", minWidth: 28, textAlign: "center", fontWeight: "900" },\n  fontStyleRow: { flexDirection: "row", gap: 6 },\n  fontStyleButton: { flex: 1, borderRadius: 10, backgroundColor: "#e9e3d5", paddingVertical: 8, alignItems: "center" },\n  fontStyleButtonActive: { backgroundColor: "#2d6555" },\n  fontStyleText: { color: "#655d50", fontSize: 9.5, fontWeight: "900" },\n  fontStyleTextActive: { color: "#fff" },\n  cardFeatureRow: { flexDirection: "row", gap: 7 },\n  cardFeature: { flex: 1, borderRadius: 11, backgroundColor: "#e7efe9", paddingVertical: 9, alignItems: "center", borderWidth: 1, borderColor: "#c7d8ce" },\n  cardFeatureActive: { backgroundColor: "#2d6555", borderColor: "#2d6555" },\n  cardFeatureText: { color: "#365d50", fontSize: 9.5, fontWeight: "900" },\n  cardFeatureTextActive: { color: "#fff" },\n  interactiveAyahList: { marginTop: 12, gap: 10 },\n  tapWordHint: { color: "#8b7651", fontSize: 9.5, fontWeight: "800", textAlign: "center", marginBottom: 2 },\n  interactiveAyah: { borderRadius: 14, backgroundColor: "#fffdf7", borderWidth: 1, borderColor: "#e7dcc5", padding: 10 },\n  wordWrap: { flexDirection: "row-reverse", flexWrap: "wrap", alignItems: "baseline", justifyContent: "flex-start", gap: 2 },\n  wordButton: { paddingHorizontal: 2, paddingVertical: 1, borderRadius: 6 },\n  wordButtonText: { color: "#183e35", textAlign: "right", writingDirection: "rtl" },\n  ayahNumber: { color: "#9b762b", fontWeight: "900", marginHorizontal: 4 },\n  translationBox: { marginTop: 8, borderRadius: 11, backgroundColor: "#edf5f1", padding: 9 },\n  translationLabel: { color: "#9b762b", fontSize: 8, fontWeight: "900", letterSpacing: .7 },\n  translationText: { color: "#405c52", fontSize: 11.5, lineHeight: 17, marginTop: 3 },\n  quizPanel: { marginTop: 12, borderRadius: 16, backgroundColor: "#fff6cf", borderWidth: 1, borderColor: "#e2c969", padding: 11 },\n  quizHeader: { marginBottom: 8 }, quizTitle: { color: "#37574c", fontSize: 14, fontWeight: "900" }, quizSub: { color: "#83765d", fontSize: 9.5, marginTop: 2 },\n  quizModeRow: { flexDirection: "row", gap: 7 }, quizMode: { flex: 1, borderRadius: 11, backgroundColor: "#2d6555", paddingVertical: 9, alignItems: "center" }, quizModeText: { color: "#fff", fontSize: 9.5, fontWeight: "900" },\n  quizQuestion: { marginTop: 10, borderRadius: 13, backgroundColor: "#fffdf5", borderWidth: 1, borderColor: "#e0d3ad", padding: 10 }, quizQuestionType: { color: "#9a7428", fontSize: 8, fontWeight: "900", letterSpacing: .8 }, quizPrompt: { color: "#254b3f", fontSize: 17, lineHeight: 29, textAlign: "right", writingDirection: "rtl", fontWeight: "800", marginTop: 6 },\n  quizOptions: { gap: 6, marginTop: 9 }, quizOption: { borderRadius: 10, backgroundColor: "#eee8da", padding: 9, borderWidth: 1, borderColor: "#ddd0b5" }, quizOptionCorrect: { backgroundColor: "#dff2e4", borderColor: "#79b98a" }, quizOptionWrong: { backgroundColor: "#f8dfd9", borderColor: "#d79589" }, quizOptionText: { color: "#43594f", fontSize: 11.5, fontWeight: "800", textAlign: "right" }, quizOptionTextCorrect: { color: "#28603b" }, quizFeedback: { marginTop: 8, fontSize: 10.5, fontWeight: "900", textAlign: "center" }, quizFeedbackGood: { color: "#2c7047" }, quizFeedbackTry: { color: "#9a5549" }, nextQuizButton: { alignSelf: "center", marginTop: 8, borderRadius: 9, backgroundColor: "#e8e1cf", paddingHorizontal: 11, paddingVertical: 7 }, nextQuizText: { color: "#5b6159", fontSize: 9.5, fontWeight: "900" },\n`;
s = s.replace(styleAnchor, newStyles + styleAnchor);

fs.writeFileSync(path, s);
console.log('Applied interactive Al-Hafiz study cards');
