import fs from "node:fs";

const displayPath = new URL("../src/MasjidTvDisplay.tsx", import.meta.url);
let source = fs.readFileSync(displayPath, "utf8");

const replaceOnce = (from, to, label) => {
  if (source.includes(to)) return;
  if (!source.includes(from)) throw new Error(`Missing source for ${label}`);
  source = source.replace(from, to);
};

source = source.replace(
  'type LandscapeLayout = "grand" | "community" | "minimal";',
  'type LandscapeLayout = "grand" | "community" | "minimal" | "announcements" | "cinematic" | "board" | "jumuah" | "adaptive";'
);
if (!source.includes('type SpecialDisplayMode =')) {
  source = source.replace(
    'type BackgroundMode = "theme" | "kaabaVideo";\n',
    'type BackgroundMode = "theme" | "kaabaVideo";\ntype SpecialDisplayMode = "auto" | "daily" | "jumuah" | "ramadan" | "eid" | "fundraiser" | "janazah" | "emergency";\n'
  );
}

replaceOnce(
  '  backgroundDim: number;\n  theme: "emerald" | "midnight" | "ivory";\n',
  '  backgroundDim: number;\n  specialMode: SpecialDisplayMode;\n  showClock: boolean;\n  showDate: boolean;\n  showLocation: boolean;\n  showLogo: boolean;\n  showNextPrayer: boolean;\n  showPrayerCards: boolean;\n  showFooter: boolean;\n  showConnectedStatus: boolean;\n  showVerse: boolean;\n  showDonation: boolean;\n  verseText: string;\n  websiteUrl: string;\n  donationLabel: string;\n  donationQrUrl: string;\n  backgroundColor: string;\n  cardColor: string;\n  accentColor: string;\n  textColor: string;\n  theme: "emerald" | "midnight" | "ivory";\n',
  'smart settings fields'
);

replaceOnce(
  '  backgroundDim: 0.45,\n  theme: "emerald",\n',
  '  backgroundDim: 0.45,\n  specialMode: "auto",\n  showClock: true,\n  showDate: true,\n  showLocation: true,\n  showLogo: true,\n  showNextPrayer: true,\n  showPrayerCards: true,\n  showFooter: true,\n  showConnectedStatus: true,\n  showVerse: false,\n  showDonation: false,\n  verseText: "Indeed, in the remembrance of Allah do hearts find rest. — Qur’an 13:28",\n  websiteUrl: "",\n  donationLabel: "Support your Masjid",\n  donationQrUrl: "",\n  backgroundColor: "#052B24",\n  cardColor: "#073B32",\n  accentColor: "#F6D675",\n  textColor: "#FFFFFF",\n  theme: "emerald",\n',
  'smart defaults'
);

// Smart local admin controls. These are deliberately data-driven so every layout can reuse them.
const layoutAnchor = '<Text style={styles.adminSection}>Landscape layouts</Text><View style={styles.choiceRow}>{(["grand", "community", "minimal"] as LandscapeLayout[]).map((item) => <Pressable key={item} onPress={() => update({ landscapeLayout: item })} style={[styles.choice, settings.landscapeLayout === item && styles.choiceActive]}><Text style={styles.choiceText}>{item}</Text></Pressable>)}</View>';
const smartControls = `<Text style={styles.adminSection}>Masjid Display Studio — layout library</Text><View style={styles.choiceRow}>{(["grand", "community", "announcements", "cinematic", "board", "jumuah", "adaptive", "minimal"] as LandscapeLayout[]).map((item) => <Pressable key={item} onPress={() => update({ landscapeLayout: item })} style={[styles.choice, settings.landscapeLayout === item && styles.choiceActive]}><Text style={styles.choiceText}>{item}</Text></Pressable>)}</View>\n              <Text style={styles.adminSection}>Special display mode</Text><View style={styles.choiceRow}>{(["auto","daily","jumuah","ramadan","eid","fundraiser","janazah","emergency"] as SpecialDisplayMode[]).map((item) => <Pressable key={item} onPress={() => update({ specialMode: item })} style={[styles.choice, settings.specialMode === item && styles.choiceActive]}><Text style={styles.choiceText}>{item}</Text></Pressable>)}</View>\n              <Text style={styles.adminSection}>Show / hide widgets</Text>\n              <ToggleRow label="Mosque logo" value={settings.showLogo} onChange={(showLogo) => update({ showLogo })} />\n              <ToggleRow label="Clock" value={settings.showClock} onChange={(showClock) => update({ showClock })} />\n              <ToggleRow label="Date" value={settings.showDate} onChange={(showDate) => update({ showDate })} />\n              <ToggleRow label="Location" value={settings.showLocation} onChange={(showLocation) => update({ showLocation })} />\n              <ToggleRow label="Next prayer" value={settings.showNextPrayer} onChange={(showNextPrayer) => update({ showNextPrayer })} />\n              <ToggleRow label="Five prayer times" value={settings.showPrayerCards} onChange={(showPrayerCards) => update({ showPrayerCards })} />\n              <ToggleRow label="Announcements" value={settings.showAnnouncements} onChange={(showAnnouncements) => update({ showAnnouncements })} />\n              <ToggleRow label="Jumu’ah" value={settings.showJumuah} onChange={(showJumuah) => update({ showJumuah })} />\n              <ToggleRow label="Iqama" value={settings.showIqama} onChange={(showIqama) => update({ showIqama })} />\n              <ToggleRow label="Qur’an / reminder text" value={settings.showVerse} onChange={(showVerse) => update({ showVerse })} />\n              <ToggleRow label="Donation / website widget" value={settings.showDonation} onChange={(showDonation) => update({ showDonation })} />\n              <ToggleRow label="Footer" value={settings.showFooter} onChange={(showFooter) => update({ showFooter })} />\n              <ToggleRow label="Connected status" value={settings.showConnectedStatus} onChange={(showConnectedStatus) => update({ showConnectedStatus })} />\n              <Text style={styles.adminSection}>Smart theme colors</Text>\n              <Text style={styles.adminLabel}>Background</Text><TextInput value={settings.backgroundColor} onChangeText={(backgroundColor) => update({ backgroundColor })} placeholder="#052B24" style={styles.input} />\n              <Text style={styles.adminLabel}>Cards</Text><TextInput value={settings.cardColor} onChangeText={(cardColor) => update({ cardColor })} placeholder="#073B32" style={styles.input} />\n              <Text style={styles.adminLabel}>Accent / gold</Text><TextInput value={settings.accentColor} onChangeText={(accentColor) => update({ accentColor })} placeholder="#F6D675" style={styles.input} />\n              <Text style={styles.adminLabel}>Main text</Text><TextInput value={settings.textColor} onChangeText={(textColor) => update({ textColor })} placeholder="#FFFFFF" style={styles.input} />\n              <Text style={styles.adminLabel}>Qur’an / reminder</Text><TextInput value={settings.verseText} onChangeText={(verseText) => update({ verseText })} style={styles.input} />\n              <Text style={styles.adminLabel}>Masjid website</Text><TextInput value={settings.websiteUrl} onChangeText={(websiteUrl) => update({ websiteUrl })} autoCapitalize="none" style={styles.input} />\n              <Text style={styles.adminLabel}>Donation label</Text><TextInput value={settings.donationLabel} onChangeText={(donationLabel) => update({ donationLabel })} style={styles.input} />\n              <Text style={styles.adminLabel}>Donation QR/image URL</Text><TextInput value={settings.donationQrUrl} onChangeText={(donationQrUrl) => update({ donationQrUrl })} autoCapitalize="none" style={styles.input} />`;
if (source.includes(layoutAnchor)) source = source.replace(layoutAnchor, smartControls);
else if (!source.includes('Masjid Display Studio — layout library')) throw new Error('Display Studio local controls anchor missing');

const bodyAnchor = '  const body = landscape\n    ? layout === "community" ? <CommunityLandscape /> : layout === "minimal" ? <MinimalLandscape /> : <GrandLandscape />\n    : layout === "lobby" ? <LobbyPortrait /> : layout === "minimal" ? <MinimalPortrait /> : <MinaretPortrait />;';
const smartBody = `  const smartCard = { backgroundColor: settings.cardColor, borderColor: settings.accentColor };\n  const smartText = { color: settings.textColor };\n  const smartAccent = { color: settings.accentColor };\n  const effectiveLayout: LandscapeLayout = settings.specialMode === "jumuah" ? "jumuah" : settings.specialMode === "fundraiser" ? "announcements" : settings.specialMode === "emergency" ? "announcements" : settings.landscapeLayout;\n\n  const SmartBrand = () => <View style={styles.smartBrand}>{settings.showLogo && settings.mosqueLogoUri ? <Image source={{ uri: settings.mosqueLogoUri }} style={styles.smartLogo} resizeMode="contain" /> : null}<View><Text style={[styles.smartMasjidName, smartAccent]}>{settings.mosqueName}</Text>{settings.showLocation ? <Text style={[styles.smartSmall, smartText]}>⌖ {displayLocationLabel}</Text> : null}</View></View>;\n  const SmartClock = () => settings.showClock ? <Pressable onPress={() => setAdminOpen(true)} accessibilityRole="button" accessibilityLabel="Open masjid setup"><Text style={[styles.smartClock, smartText]}>{displayClock(props.now, settings.showSeconds)}</Text></Pressable> : null;\n  const SmartDate = () => settings.showDate ? <View style={styles.smartDateBox}><Text style={[styles.smartDate, smartText]}>{props.shortDate}</Text>{settings.showHijri ? <Text style={[styles.smartHijri, smartAccent]}>{props.hijriDate}</Text> : null}</View> : null;\n  const SmartNext = () => settings.showNextPrayer ? <View style={[styles.smartNext, smartCard]}><View><Text style={[styles.smartKicker, smartAccent]}>NEXT PRAYER</Text><Text style={[styles.smartPrayerArabic, smartText]}>{NAMES[nextPrayer].ar}</Text><Text style={[styles.smartPrayerEnglish, smartAccent]}>{NAMES[nextPrayer].en}</Text></View><View style={styles.smartNextTime}><Text style={[styles.smartBigTime, smartText]}>{prettyTime(nextTime)}</Text>{props.next ? <Text style={[styles.smartCountdown, smartAccent]}>{countdown(props.next.secondsRemaining)} left</Text> : null}{settings.showIqama ? <Text style={[styles.smartIqama, smartText]}>Iqama {nextIqama === "—" ? "—" : prettyTime(nextIqama)}</Text> : null}</View></View> : null;\n  const SmartPrayerList = ({ cards = false }: { cards?: boolean }) => settings.showPrayerCards ? <View style={cards ? styles.smartPrayerCards : styles.smartPrayerTable}>{PRAYER_KEYS.map((prayer) => <View key={prayer} style={[cards ? styles.smartPrayerMiniCard : styles.smartPrayerRow, smartCard, prayer === nextPrayer && { borderColor: settings.accentColor, borderWidth: 2 }]}><Text style={[styles.smartPrayerName, smartText]}>{ICONS[prayer]} {NAMES[prayer].en} <Text style={smartAccent}>{NAMES[prayer].ar}</Text></Text><Text style={[styles.smartPrayerValue, smartText]}>Adhan {prettyTime(todayTimes[prayer] || "—")}</Text>{settings.showIqama ? <Text style={[styles.smartPrayerIqama, smartAccent]}>Iqama {todayIqama[prayer] ? prettyTime(todayIqama[prayer]) : "—"}</Text> : null}</View>)}</View> : null;\n  const SmartAnnouncements = () => settings.showAnnouncements ? <View style={[styles.smartPanel, smartCard]}><Text style={[styles.smartPanelTitle, smartAccent]}>📣 ANNOUNCEMENTS</Text>{announcements.length ? announcements.slice(0, 5).map((item) => <View key={item.id} style={styles.smartAnnouncement}><Text style={[styles.smartAnnouncementTitle, item.priority === "emergency" ? { color: "#FF8A80" } : smartText]}>{item.title}</Text><Text style={[styles.smartAnnouncementBody, smartText]}>{item.body}</Text></View>) : <Text style={[styles.smartEmpty, smartText]}>No announcements today</Text>}</View> : null;\n  const SmartJumuah = () => settings.showJumuah ? <View style={[styles.smartPanel, smartCard]}><Text style={[styles.smartPanelTitle, smartAccent]}>JUMU’AH</Text>{settings.jumuah.filter((j) => j.time).length ? settings.jumuah.filter((j) => j.time).slice(0, 4).map((j) => <View key={j.id} style={styles.smartJumuahRow}><View><Text style={[styles.smartAnnouncementTitle, smartText]}>{j.label}</Text><Text style={[styles.smartSmall, smartText]}>{[j.imam, j.language].filter(Boolean).join(" • ")}</Text></View><Text style={[styles.smartJumuahTime, smartAccent]}>{j.time}</Text></View>) : <Text style={[styles.smartEmpty, smartText]}>Jumu’ah times not set</Text>}</View> : null;\n  const SmartDonation = () => settings.showDonation ? <View style={[styles.smartDonation, smartCard]}><Text style={[styles.smartPanelTitle, smartAccent]}>{settings.donationLabel}</Text>{settings.donationQrUrl ? <Image source={{ uri: settings.donationQrUrl }} style={styles.smartQr} resizeMode="contain" /> : null}{settings.websiteUrl ? <Text style={[styles.smartWebsite, smartText]}>{settings.websiteUrl}</Text> : null}</View> : null;\n  const SmartVerse = () => settings.showVerse && settings.verseText ? <Text style={[styles.smartVerse, smartText]}>{settings.verseText}</Text> : null;\n\n  const SmartLandscape = ({ variant }: { variant: LandscapeLayout }) => {\n    const cinematic = variant === "cinematic";\n    const announcementFirst = variant === "announcements" || settings.specialMode === "emergency" || settings.specialMode === "fundraiser";\n    const jumuahFirst = variant === "jumuah";\n    const cards = variant === "grand" || variant === "cinematic" || variant === "community";\n    if (variant === "minimal") return <View style={[styles.smartCanvas, { backgroundColor: settings.backgroundColor }]}><View style={styles.smartHeader}><SmartBrand /><SmartClock /><SmartDate /></View><SmartNext /><SmartPrayerList cards /><SmartVerse /></View>;\n    return <View style={[styles.smartCanvas, { backgroundColor: settings.backgroundColor }]}><View style={styles.smartHeader}><SmartBrand /><SmartClock /><SmartDate /></View>{cinematic ? <SmartNext /> : null}<View style={[styles.smartMain, announcementFirst && styles.smartMainAnnouncementFirst]}><View style={[styles.smartColumn, { flex: announcementFirst ? 0.8 : 1 }]}>{!cinematic ? <SmartNext /> : null}{jumuahFirst ? <SmartJumuah /> : <SmartPrayerList cards={cards} />}{settings.showVerse ? <SmartVerse /> : null}</View><View style={[styles.smartColumn, { flex: announcementFirst ? 1.35 : 1 }]}>{announcementFirst ? <SmartAnnouncements /> : jumuahFirst ? <><SmartJumuah /><SmartAnnouncements /></> : <><SmartAnnouncements /><SmartJumuah /></>}<SmartDonation /></View></View>{cinematic ? <SmartPrayerList cards /> : null}</View>;\n  };\n\n  const SmartPortrait = () => <ScrollView contentContainerStyle={[styles.smartPortrait, { backgroundColor: settings.backgroundColor }]}><SmartBrand /><SmartClock /><SmartDate /><SmartNext /><SmartPrayerList /><SmartAnnouncements /><SmartJumuah /><SmartDonation /><SmartVerse /></ScrollView>;\n\n  const body = landscape ? <SmartLandscape variant={effectiveLayout} /> : <SmartPortrait />;`;
if (source.includes(bodyAnchor)) source = source.replace(bodyAnchor, smartBody);
else if (!source.includes('const SmartLandscape =')) throw new Error('Smart body anchor missing');

// Footer visibility and status visibility.
source = source.replace(
  '<View style={styles.footer}><Text style={styles.footerText}>{settings.mosqueSubtitle}</Text><Text style={styles.footerCopyright}>© Hassoun</Text><Text style={styles.footerStatus}>{remoteOnline ? "● Connected" : "○ Offline-safe"}</Text></View>',
  '{settings.showFooter ? <View style={styles.footer}><Text style={styles.footerText}>{settings.mosqueSubtitle}</Text><Text style={styles.footerCopyright}>© Hassoun</Text>{settings.showConnectedStatus ? <Text style={styles.footerStatus}>{remoteOnline ? "● Connected" : "○ Offline-safe"}</Text> : null}</View> : null}'
);

// New remote quick commands.
const commandAnchor = '            if (command === "orientation_portrait") setSettings((current) => ({ ...current, orientationMode: "portrait" }));\n';
if (source.includes(commandAnchor) && !source.includes('layout_cinematic')) {
  source = source.replace(commandAnchor, commandAnchor + '            if (command === "layout_announcements") setSettings((current) => ({ ...current, landscapeLayout: "announcements" }));\n            if (command === "layout_cinematic") setSettings((current) => ({ ...current, landscapeLayout: "cinematic" }));\n            if (command === "layout_board") setSettings((current) => ({ ...current, landscapeLayout: "board" }));\n            if (command === "layout_jumuah") setSettings((current) => ({ ...current, landscapeLayout: "jumuah" }));\n            if (command === "layout_adaptive") setSettings((current) => ({ ...current, landscapeLayout: "adaptive" }));\n            if (command.startsWith("mode_")) setSettings((current) => ({ ...current, specialMode: command.slice(5) as SpecialDisplayMode }));\n');
}

// Smart display styles.
const styleAnchor = '  screen: { flex: 1, backgroundColor: "#052B24" },';
if (source.includes(styleAnchor) && !source.includes('smartCanvas:')) {
  source = source.replace(styleAnchor, styleAnchor + ' smartCanvas: { flex: 1, paddingHorizontal: 24, paddingTop: 18, gap: 14 }, smartPortrait: { flexGrow: 1, padding: 22, gap: 16 }, smartHeader: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", minHeight: 112 }, smartBrand: { flexDirection: "row", alignItems: "center", gap: 12, flex: 1 }, smartLogo: { width: 72, height: 72 }, smartMasjidName: { fontSize: 30, fontWeight: "900" }, smartSmall: { fontSize: 14, opacity: 0.78, marginTop: 3 }, smartClock: { fontSize: 64, fontWeight: "900", letterSpacing: 1 }, smartDateBox: { alignItems: "flex-end", flex: 1 }, smartDate: { fontSize: 19, fontWeight: "800" }, smartHijri: { fontSize: 16, fontWeight: "800", marginTop: 6 }, smartNext: { borderWidth: 1.5, borderRadius: 24, paddingHorizontal: 24, paddingVertical: 16, flexDirection: "row", justifyContent: "space-between", alignItems: "center", minHeight: 145 }, smartKicker: { fontSize: 14, fontWeight: "900", letterSpacing: 1.5 }, smartPrayerArabic: { fontSize: 42, fontWeight: "900", marginTop: 4 }, smartPrayerEnglish: { fontSize: 25, fontWeight: "900" }, smartNextTime: { alignItems: "flex-end" }, smartBigTime: { fontSize: 55, fontWeight: "900" }, smartCountdown: { fontSize: 19, fontWeight: "900", marginTop: 4 }, smartIqama: { fontSize: 17, fontWeight: "800", marginTop: 5 }, smartMain: { flex: 1, flexDirection: "row", gap: 14, minHeight: 0 }, smartMainAnnouncementFirst: { flexDirection: "row-reverse" }, smartColumn: { gap: 12, minHeight: 0 }, smartPanel: { borderWidth: 1, borderRadius: 22, padding: 16, flexShrink: 1 }, smartPanelTitle: { fontSize: 20, fontWeight: "900", marginBottom: 10 }, smartAnnouncement: { borderTopWidth: 1, borderTopColor: "#FFFFFF22", paddingVertical: 9 }, smartAnnouncementTitle: { fontSize: 18, fontWeight: "900" }, smartAnnouncementBody: { fontSize: 14, opacity: 0.82, marginTop: 2 }, smartEmpty: { fontSize: 15, opacity: 0.7, paddingVertical: 12 }, smartJumuahRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", paddingVertical: 8, borderTopWidth: 1, borderTopColor: "#FFFFFF22" }, smartJumuahTime: { fontSize: 24, fontWeight: "900" }, smartPrayerTable: { borderRadius: 22, overflow: "hidden", gap: 6 }, smartPrayerRow: { borderWidth: 1, borderRadius: 14, paddingHorizontal: 14, paddingVertical: 9, flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 10 }, smartPrayerName: { fontSize: 17, fontWeight: "900", flex: 1 }, smartPrayerValue: { fontSize: 15, fontWeight: "800" }, smartPrayerIqama: { fontSize: 15, fontWeight: "900" }, smartPrayerCards: { flexDirection: "row", gap: 8 }, smartPrayerMiniCard: { flex: 1, borderWidth: 1, borderRadius: 16, padding: 10, minHeight: 92, justifyContent: "space-between" }, smartDonation: { borderWidth: 1, borderRadius: 22, padding: 14, alignItems: "center" }, smartQr: { width: 115, height: 115, backgroundColor: "#FFFFFF", borderRadius: 10 }, smartWebsite: { fontSize: 16, fontWeight: "800", marginTop: 7 }, smartVerse: { fontSize: 15, fontStyle: "italic", opacity: 0.85, textAlign: "center", paddingVertical: 7 },');
}

fs.writeFileSync(displayPath, source);

// Extend the paired-phone Masjid control panel with the same Display Studio settings.
const remotePath = new URL("../src/WallRemoteController.tsx", import.meta.url);
let remote = fs.readFileSync(remotePath, "utf8");
remote = remote.replace('On the wall tablet, hold the large clock → Remote.', 'On the Masjid TV, click/tap the large clock → Remote.');
remote = remote.replace(
  '["grand","community","minimal"].map((layout)',
  '["grand","community","announcements","cinematic","board","jumuah","adaptive","minimal"].map((layout)'
);
const remoteToggleAnchor = '<Toggle label="Show announcements" value={draft.showAnnouncements !== false} onChange={(showAnnouncements) => patch({ showAnnouncements })} />';
if (remote.includes(remoteToggleAnchor) && !remote.includes('Ultimate smart display widgets')) {
  const extra = `${remoteToggleAnchor}\n            <Text style={styles.section}>Ultimate smart display widgets</Text>\n            <Toggle label="Show mosque logo" value={draft.showLogo !== false} onChange={(showLogo) => patch({ showLogo })} />\n            <Toggle label="Show clock" value={draft.showClock !== false} onChange={(showClock) => patch({ showClock })} />\n            <Toggle label="Show date" value={draft.showDate !== false} onChange={(showDate) => patch({ showDate })} />\n            <Toggle label="Show location" value={draft.showLocation !== false} onChange={(showLocation) => patch({ showLocation })} />\n            <Toggle label="Show next prayer" value={draft.showNextPrayer !== false} onChange={(showNextPrayer) => patch({ showNextPrayer })} />\n            <Toggle label="Show five prayer times" value={draft.showPrayerCards !== false} onChange={(showPrayerCards) => patch({ showPrayerCards })} />\n            <Toggle label="Show Qur’an/reminder" value={Boolean(draft.showVerse)} onChange={(showVerse) => patch({ showVerse })} />\n            <Toggle label="Show donation/website" value={Boolean(draft.showDonation)} onChange={(showDonation) => patch({ showDonation })} />\n            <Toggle label="Show footer" value={draft.showFooter !== false} onChange={(showFooter) => patch({ showFooter })} />\n            <Toggle label="Show connected status" value={draft.showConnectedStatus !== false} onChange={(showConnectedStatus) => patch({ showConnectedStatus })} />\n            <Text style={styles.section}>Special display mode</Text><View style={styles.presetRow}>{["auto","daily","jumuah","ramadan","eid","fundraiser","janazah","emergency"].map((mode) => <Pressable key={mode} onPress={() => patch({ specialMode: mode })} style={[styles.preset, draft.specialMode === mode && styles.presetActive]}><Text style={styles.presetText}>{mode}</Text></Pressable>)}</View>\n            <ColorRow label="Masjid background" value={draft.backgroundColor || "#052B24"} onChange={(backgroundColor) => patch({ backgroundColor })} />\n            <ColorRow label="Card color" value={draft.cardColor || "#073B32"} onChange={(cardColor) => patch({ cardColor })} />\n            <ColorRow label="Accent color" value={draft.accentColor || "#F6D675"} onChange={(accentColor) => patch({ accentColor })} />\n            <ColorRow label="Text color" value={draft.textColor || "#FFFFFF"} onChange={(textColor) => patch({ textColor })} />\n            <Text style={styles.label}>Qur’an / reminder text</Text><TextInput value={draft.verseText || ""} onChangeText={(verseText) => patch({ verseText })} style={styles.wideInput} />\n            <Text style={styles.label}>Website</Text><TextInput value={draft.websiteUrl || ""} onChangeText={(websiteUrl) => patch({ websiteUrl })} autoCapitalize="none" style={styles.wideInput} />\n            <Text style={styles.label}>Donation label</Text><TextInput value={draft.donationLabel || ""} onChangeText={(donationLabel) => patch({ donationLabel })} style={styles.wideInput} />\n            <Text style={styles.label}>Donation QR/image URL</Text><TextInput value={draft.donationQrUrl || ""} onChangeText={(donationQrUrl) => patch({ donationQrUrl })} autoCapitalize="none" style={styles.wideInput} />`;
  remote = remote.replace(remoteToggleAnchor, extra);
}
remote = remote.replace(
  '<Pressable style={styles.command} onPress={() => void command("layout_minimal", "Minimal layout selected.")}><Text style={styles.commandTitle}>Minimal</Text></Pressable>',
  '<Pressable style={styles.command} onPress={() => void command("layout_minimal", "Minimal layout selected.")}><Text style={styles.commandTitle}>Minimal</Text></Pressable><Pressable style={styles.command} onPress={() => void command("layout_cinematic", "Cinematic layout selected.")}><Text style={styles.commandTitle}>Cinematic</Text></Pressable><Pressable style={styles.command} onPress={() => void command("layout_announcements", "Announcements layout selected.")}><Text style={styles.commandTitle}>Announcements</Text></Pressable><Pressable style={styles.command} onPress={() => void command("layout_jumuah", "Jumu’ah layout selected.")}><Text style={styles.commandTitle}>Jumu’ah</Text></Pressable>'
);
fs.writeFileSync(remotePath, remote);

console.log("Applied ultimate Masjid Display Studio layouts, widgets, colors, special modes, and remote controls");
